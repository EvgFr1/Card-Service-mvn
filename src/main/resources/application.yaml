spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CardService
    username: postgres
    password: Zhenya123
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
